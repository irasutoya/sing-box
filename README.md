```shell
bash <(curl -fsSL https://ie.cx/install.sh)
```

```shell
Usage: install.sh [options]

Options:
  -dir         设置文件存放的基础目录 (默认: /root/hysteria2)
  -port        设置监听端口 (默认: 443)
  -domain      设置伪装域名 (默认: bing.com)
  -password    设置访问密码 (默认: 随机生成)
  -help        显示帮助信息
```
